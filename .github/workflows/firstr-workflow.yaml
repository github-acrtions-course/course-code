name: First Workflow
on: [push]

jobs:
    
    run-shell-command:
        runs-on: ubuntu-latest
        steps:
            - name: echo a string
              run: echo "Hello, world finally I started acting ..."
            - name: Multiple commands
              run: |
                    node -v 
                    npm -v


        


